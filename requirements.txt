fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy==2.0.40
pyyaml==6.0.2
httpx==0.28.1
numpy==2.2.4
onnxruntime==1.21.0
tokenizers==0.21.1
transformers==4.50.3
sentence-transformers==4.1.0
sentencepiece==0.2.0
# torch pinned to 2.5.1 for whisperx 3.3.1 + pyannote.audio 3.3.2
# compatibility: torchaudio 2.11 removed AudioMetaData /
# list_audio_backends etc. which pyannote still references. Upgrade
# to pyannote 4.x would break whisperx. Revisit when whisperx ships
# a torch 2.11-compatible release.
torch==2.5.1
torchaudio==2.5.1
# pyannote.audio 3.3.2 lazily imports matplotlib from tasks/segmentation
# — it's a transitive dep that used to come in via a pin upgrade. Pin
# explicitly so rebuilds don't drop it.
matplotlib==3.10.8
faster-whisper==1.1.0
whisperx==3.3.1
open-clip-torch==2.30.0
pillow==11.1.0
PyMuPDF==1.25.5
pymupdf4llm==0.0.17
janome==0.5.0
openai>=1.0.0
# Phase 2A cloud transcription providers (lazy-imported in providers/gemini.py)
google-genai>=1.10
# Phase 2A: grapheme-cluster split for synthetic word timestamps in
# Gemini provider. stdlib re lacks the \X pattern.
regex>=2024.11.6
jinja2>=3.1
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=1.0.0
beautifulsoup4>=4.12
html2text>=2024.2.26
lxml>=5.0
